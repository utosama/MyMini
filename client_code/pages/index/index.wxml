<!--index.wxml-->
<!-- <swiper indicator-dots="{{indicatorDots}}" style="width:100%; height:400rpx"
    autoplay="{{autoplay}}" circular="true" interval="{{interval}}" duration="{{duration}}">
    <block wx:for="{{imgUrls}}" wx:key="{{index}}">
      <swiper-item bindtap="Buy" data-index="{{index}}">
        <image src="{{item}}" class="slide-image" style="width:100%; height:400rpx"/>
        
      </swiper-item>
    </block>
</swiper>  -->
<view class='content-contain'>
  <view class='price-hint'>恭喜获得,购物立减</view>
  <view class='price'>{{price}}元</view>
  <view class='price-hint' style='padding-top:0rpx;'>需由店员核销</view>
</view>
 <view class='hint'>请收银员操作请勿点击</view>
  <view class='saler-group'>
  <view class='saler' hover-class='hover' wx:for="{{salers}}" wx:key="key" bindtap='click' data-sid='{{item.id}}'>{{item.name}}</view>
</view>   